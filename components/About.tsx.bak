
import React, { useEffect } from 'react';
import { motion } from 'framer-motion';
import { Shield, Zap, Target, Users, Plus, Image as ImageIcon } from 'lucide-react';

interface AboutProps {
  onContactClick: () => void;
  onHomeClick: () => void;
}

const ImageSoon = ({ className }: { className?: string }) => (
  <div className={`flex flex-col items-center justify-center bg-white/[0.03] border border-white/5 ${className}`}>
    <ImageIcon size={24} className="text-white/10 mb-2" />
    <span className="text-[10px] font-mono tracking-[0.4em] text-white/20 uppercase">Image Soon</span>
  </div>
);

const ImageSoonLight = ({ className }: { className?: string }) => (
  <div className={`flex flex-col items-center justify-center bg-black/[0.03] border border-black/5 ${className}`}>
    <ImageIcon size={24} className="text-black/10 mb-2" />
    <span className="text-[10px] font-mono tracking-[0.4em] text-black/20 uppercase">Image Soon</span>
  </div>
);

const TEAM_MEMBERS = [
  { name: "SANTIAGO CASTRO", role: "Founder & CEO", img: "" },
  { name: "SANTIAGO MORALES", role: "Creative Lead", img: "" },
  { name: "STHEFANIA CASTANO", role: "Ops Director", img: "" },
  { name: "LEONARDO GOMEZ", role: "Tech Lead", img: "" },
  { name: "DANIELLA POSADA", role: "Brand Strategist", img: "" },
  { name: "JHONATAN GOMEZ", role: "Account Manager", img: "" },
  { name: "JUAN DIEGO BOTERO", role: "Dev Lead", img: "" },
  { name: "ESTEBAN CORREA", role: "Creative", img: "" },
  { name: "JULIAN AGUDELO", role: "Designer", img: "" },
  { name: "ALEJANDRA OSPINA", role: "Visual Designer", img: "" },
  { name: "MATEO URREGO", role: "Video Editor", img: "" }
];

const VALUES = [
  { title: "INTEGRITY", desc: "Honesty and ethics are at our core. We build trust through transparency.", icon: Shield },
  { title: "RELENTLESS", desc: "We don't stop until we achieve the impossible for our clients.", icon: Zap },
  { title: "GROWTH MINDED", desc: "Constantly evolving. We thrive on challenges and continuous learning.", icon: Target },
  { title: "INTENTIONAL", desc: "Every decision is calculated for maximum brand impact.", icon: Users }
];

export const About: React.FC<AboutProps> = ({ onContactClick, onHomeClick }) => {
  useEffect(() => {
    window.scrollTo({ top: 0, behavior: 'instant' });
  }, []);

  return (
    <div className="min-h-screen font-sans overflow-x-hidden">

      {/* SECTION 1: DARK HEADER */}
      <section className="bg-tech-black text-white pt-40 pb-40 relative overflow-hidden">
        <div className="absolute inset-0 z-0 opacity-[0.02] pointer-events-none"
          style={{ backgroundImage: 'linear-gradient(to right, white 1px, transparent 1px), linear-gradient(to bottom, white 1px, transparent 1px)', backgroundSize: '60px 60px' }} />

        <div className="max-w-7xl mx-auto px-6 relative z-10 text-center">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 1 }}
            className="space-y-8"
          >
            <span className="text-[10px] font-mono tracking-[1em] text-tech-accent uppercase block">Collective // 01</span>
            <h1 className="text-7xl md:text-[11rem] font-thin tracking-[-0.05em] uppercase leading-[0.8] text-white">
              BEYOND <br />
              <span className="text-white/10 italic">SYSTEMS.</span>
            </h1>
          </motion.div>

          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            transition={{ delay: 0.5, duration: 1 }}
            className="mt-24 flex flex-col items-center gap-8"
          >
            <div className="w-px h-32 bg-gradient-to-b from-transparent via-white/20 to-transparent" />
            <p className="max-w-3xl text-xl md:text-2xl font-light text-white/40 tracking-tight leading-relaxed italic mx-auto">
              We are a high-performance visual strategy agency. Our mission is to architect digital dominance for brands that refuse to be ignored.
            </p>
          </motion.div>
        </div>
      </section>

      {/* SECTION 2: WHITE CONTENT (TEAM & METHODOLOGY) */}
      <section className="bg-white text-black py-60 relative overflow-hidden">
        <div className="absolute inset-0 z-0 opacity-[0.05] pointer-events-none"
          style={{ backgroundImage: 'linear-gradient(to right, black 1px, transparent 1px), linear-gradient(to bottom, black 1px, transparent 1px)', backgroundSize: '80px 80px' }} />

        <div className="max-w-7xl mx-auto px-6 relative z-10">

          {/* Methodology Header */}
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-20 mb-60 items-end">
            <div>
              <span className="text-[10px] font-mono tracking-[0.5em] text-black/40 uppercase mb-6 block">// OUR_METHODOLOGY</span>
              <h2 className="text-5xl md:text-8xl font-thin tracking-tighter uppercase leading-none">
                TECHNICAL <br />
                <span className="text-black/20 italic">PRECISION.</span>
              </h2>
            </div>
            <div className="space-y-8">
              <p className="text-xl font-light text-black/60 leading-relaxed max-w-lg">
                We've refined a process that merges creative intuition with engineering logic. Every pixel is intentional, every frame is calculated.
              </p>
              <div className="flex gap-12">
                <div className="flex flex-col gap-2">
                  <span className="text-3xl font-thin tracking-tighter">14+</span>
                  <span className="text-[9px] font-mono text-black/40 uppercase tracking-widest">Global Partners</span>
                </div>
                <div className="flex flex-col gap-2">
                  <span className="text-3xl font-thin tracking-tighter">250+</span>
                  <span className="text-[9px] font-mono text-black/40 uppercase tracking-widest">Projects Sync</span>
                </div>
              </div>
            </div>
          </div>

          {/* Methodology Blocks */}
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-px bg-black/10 border border-black/10 mb-80">
            {[
              { id: "01", title: "Visual Engineering", desc: "Deep architectural logic applied to brand aesthetics. We don't just design, we build frameworks for scalability." },
              { id: "02", title: "Temporal Logic", desc: "Optimizing the consumer's time spent with your brand. Maximum visual impact in sub-second interactions." },
              { id: "03", title: "Market dominance", desc: "Aggressive visual strategies that position your brand as the undisputed leader in your vertical." }
            ].map((item, i) => (
              <motion.div
                key={i}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ duration: 0.8, delay: i * 0.1 }}
                className="bg-white p-12 space-y-8 group hover:bg-black hover:text-white transition-all duration-700 cursor-default"
              >
                <span className="text-[10px] font-mono text-black/30 group-hover:text-white/40 block tracking-widest uppercase italic">{item.id}_SYNAPSE</span>
                <div className="space-y-4">
                  <h3 className="text-2xl font-thin tracking-widest uppercase">{item.title}</h3>
                  <p className="text-sm font-light leading-relaxed opacity-60 group-hover:opacity-100 uppercase tracking-tight">{item.desc}</p>
                </div>
              </motion.div>
            ))}
          </div>

          {/* Group Image Placeholder */}
          <motion.div
            initial={{ opacity: 0 }}
            whileInView={{ opacity: 1 }}
            viewport={{ once: true }}
            transition={{ duration: 1.5 }}
            className="mb-80"
          >
            <ImageSoonLight className="w-full aspect-[21/9] border border-black/5" />
            <div className="mt-6 flex justify-between items-center text-[10px] font-mono text-black/20 uppercase tracking-[0.4em]">
              <span>Perspective // 01</span>
              <span>Studio_Environment_v2</span>
            </div>
          </motion.div>

          {/* Operatives Header */}
          <div className="flex flex-col items-center text-center mb-32">
            <h2 className="text-6xl md:text-[10rem] font-thin tracking-[-0.04em] uppercase leading-none mb-8">OPERATIVES_</h2>
            <div className="w-24 h-px bg-black/20" />
          </div>

          {/* Operatives Grid */}
          <div className="grid grid-cols-2 lg:grid-cols-4 gap-8 mb-40">
            {TEAM_MEMBERS.map((member, i) => (
              <motion.div
                key={i}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ duration: 1, delay: i * 0.05 }}
                className="group"
              >
                <div className="relative aspect-[3/4] mb-8 overflow-hidden bg-black/[0.02] border border-black/[0.05] group-hover:border-black/20 transition-all duration-700">
                  {member.img ? (
                    <img
                      src={member.img}
                      alt={member.name}
                      className="w-full h-full object-cover grayscale opacity-40 group-hover:opacity-100 group-hover:grayscale-0 transition-all duration-1000"
                    />
                  ) : (
                    <ImageSoonLight className="w-full h-full" />
                  )}
                  <div className="absolute top-4 left-4 text-[8px] font-mono text-black/20 uppercase tracking-[0.5em] group-hover:text-black/60 transition-colors">
                    Sync_0{i + 1}
                  </div>
                </div>
                <div className="space-y-1 text-center">
                  <h3 className="text-[11px] font-black tracking-[0.2em] text-black uppercase">{member.name}</h3>
                  <p className="text-[9px] font-mono text-black/30 uppercase tracking-widest">{member.role}</p>
                </div>
              </motion.div>
            ))}

            <motion.div
              onClick={onContactClick}
              className="flex flex-col items-center justify-center aspect-[3/4] bg-black text-white cursor-pointer group hover:bg-tech-accent transition-all duration-500"
            >
              <Plus size={32} className="mb-4 opacity-20 group-hover:opacity-100 group-hover:scale-125 transition-all duration-500" />
              <span className="text-[12px] font-thin tracking-[0.6em] uppercase">Connect</span>
            </motion.div>
          </div>
        </div>
      </section>

      {/* SECTION 3: DARK VALUES & CTA */}
      <section className="bg-tech-black text-white py-60 relative overflow-hidden">
        <div className="absolute inset-0 z-0 opacity-[0.03] pointer-events-none"
          style={{ backgroundImage: 'linear-gradient(to right, white 1px, transparent 1px), linear-gradient(to bottom, white 1px, transparent 1px)', backgroundSize: '100px 100px' }} />

        {/* Horizontal Guide */}
        <div className="absolute top-0 left-0 w-full h-px bg-white/5" />

        <div className="max-w-7xl mx-auto px-6 relative z-10">

          <div className="flex flex-col items-center text-center mb-32">
            <span className="text-[10px] font-mono tracking-[0.6em] text-white/20 uppercase mb-8">Our Core Principles</span>
            <h2 className="text-4xl md:text-7xl font-thin tracking-tighter uppercase italic">ENGINEERED VALUES</h2>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-px bg-white/5 border border-white/5 mb-60">
            {VALUES.map((val, i) => (
              <div key={i} className="bg-tech-black p-20 space-y-10 group hover:bg-white/[0.02] transition-colors duration-700">
                <div className="flex justify-between items-start">
                  <val.icon size={22} className="text-white/20 group-hover:text-white transition-colors duration-500" />
                  <span className="text-[9px] font-mono text-white/10 italic">VALUE_{i + 1}</span>
                </div>
                <div className="space-y-6">
                  <h3 className="text-3xl font-thin tracking-wider uppercase text-white/90">{val.title}</h3>
                  <p className="text-sm font-light text-white/30 uppercase leading-relaxed tracking-widest max-w-sm">{val.desc}</p>
                </div>
              </div>
            ))}
          </div>

          {/* Final Large CTA */}
          <div className="flex flex-col items-center border-t border-white/10 pt-60 text-center">
            <h3 className="text-5xl md:text-8xl font-thin tracking-[-0.04em] uppercase leading-none mb-24 max-w-4xl">
              READY TO <br />
              <span className="text-tech-accent italic">SYNCHRONIZE?</span>
            </h3>
            <button
              onClick={onContactClick}
              className="group relative px-20 py-10 bg-white text-black text-[12px] font-mono tracking-[0.6em] uppercase hover:bg-tech-accent hover:text-white transition-all duration-700"
            >
              <span className="relative z-10">Initialize Contact</span>
              <div className="absolute inset-x-0 bottom-0 h-0.5 bg-tech-accent translate-y-2 opacity-0 group-hover:opacity-100 group-hover:translate-y-0 transition-all duration-500" />
            </button>

            <div className="mt-40 text-[9px] font-mono text-white/10 uppercase tracking-[1em] select-none">
              Transmission // End of report
            </div>
          </div>
        </div>
      </section>

    </div>
  );
};
