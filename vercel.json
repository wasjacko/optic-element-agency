{
  "name": "optic-element-agency",
  "version": 2,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ],
  "installCommand": "npm install",
  "buildCommand": "prisma generate --schema=backend/prisma/schema.prisma && vite build"
}
